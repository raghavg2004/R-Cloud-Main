{
  "version": 2,
  "public": true,
  "buildCommand": "npm run build || true",
  "outputDirectory": "./",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
